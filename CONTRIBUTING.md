# contributing

`pip install -e .[dev]` to get started. `pytest` to run tests. `ruff check .` for linting.

The most useful contribution right now is adding new reference graphs under src/blindspot/references/. Each one is a JSON file mapping concepts to their prerequisites. See python-basics.json for the format.
